import averroes from "../api/averroes";
import { createStore } from "vuex";

// Initial state
const state = () => ({
  characters : [],
});

// getters
const getters = {
  
};

// actions
const actions = {
  
};

// mutations
const mutations = {

};

export default createStore({
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
});
